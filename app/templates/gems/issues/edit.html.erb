<div class="py-5 border-top">
  <form action="/gems/<%= current_gem.id %>/issues" method="POST">
    <input type="hidden" name="_csrf_token" value="<%= csrf_token %>"/>
    <div class="container">
      <div class="d-flex justify-content-between align-items-end mb-3">
        <div class="h2 mb-0">Select issues</div>
        <div>
          <input type="submit" class="btn btn-primary" value="Save"/>
        </div>
      </div>

      <div class="row">
        <div class="col-9">
          <div class="list-group">
            <% issues.each do |issue| %>
              <div class="list-group-item p-3">
                <div class="d-flex align-items-center">
                  <label class="d-flex align-items-center">
                    <input type="checkbox" name="issues_ids[]" value="<%= issue.id %>" class="form-check-input m-0" <%= "checked" if issue.selected %>/>

                    <span class="text-big ms-2 me-1">
                      <%= issue.title %>
                    </span>
                    <% issue.labels.each do |label| %>
                      <span class="badge ms-1 <%= 'text-black' if label.bg_light? %>" style="background: <%= label.bg_color %>"><%= label.name %></span>
                    <% end %>
                  </label>
                  <a href="<%= issue.html_url %>" class="ms-auto text-black" target="_blank">
                    <i class="bi bi-box-arrow-up-right"></i>
                  </a>
                </div>
              </div>
            <% end %>
          </div>
        </div>
        <div class="col-3">
          <div class="card">
            <div class="card-body text-muted">
              Select issues you need help with. Issues will be automatically removed from ShinyGems when you close them on GitHub.
              <br/><br/>
              Your gem will not be visible on the list if there are no issues added.
            </div>
          </div>
        </div>
      </div>
    </div>
  </form>
</div>
